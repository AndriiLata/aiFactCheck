# ── Core ───────────────────────────
Flask>=3.0
python-dotenv>=1.0
openai>=1.23

# ── DBpedia / SPARQL ───────────────
SPARQLWrapper>=2.0
rapidfuzz>=3.6

# ── NLP ────────────────────────────
spacy>=3.7                 # keep only one spacy entry
en-core-web-md==3.7.1      # ← ADD THIS LINE (or the latest 3.7.x model)
# may not load like this. ->> You might need to run `python -m spacy download en_core_web_md` separately.

sentence-transformers>=0.12

# ── Gunicorn (optional, prod) ──────
#gunicorn>=21.2

# ── Caching ────────────────────────
redis>=5.0

# ── Fact extraction ────────────────
stanford-openie>=1.3.2
